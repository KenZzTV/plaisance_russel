<!DOCTYPE html>
<html>
<head>
    <title>Détail Réservation</title>
</head>
<body>
    <h1>Détail de la réservation</h1>
    <p>ID technique : <strong><%= reservation._id %></strong></p>
    <p>Catway concerné : <strong>n°<%= reservation.catwayNumber %></strong></p>

    <form action="/catways/<%= reservation.catwayNumber %>/reservations/<%= reservation._id %>" method="POST">
        <label>Nom du Client :</label>
        <input type="text" name="clientName" value="<%= reservation.clientName %>" required>
        
        <label>Nom du Bateau :</label>
        <input type="text" name="boatName" value="<%= reservation.boatName %>" required>
        
        <label>Date de début (Départ) :</label>
        <input type="date" name="startDate" value="<%= reservation.startDate ? reservation.startDate.toISOString().split('T')[0] : '' %>" required>
        
        <label>Date de fin (Retour) :</label>
        <input type="date" name="endDate" value="<%= reservation.endDate ? reservation.endDate.toISOString().split('T')[0] : '' %>" required>
        
        <br><br>
        <button type="submit"">Enregistrer les modifications</button>
    </form>

    <hr>
    <a href="/catways/<%= reservation.catwayNumber %>/reservations">← Retour à la liste des réservations</a>
</body>
</html>